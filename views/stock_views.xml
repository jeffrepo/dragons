<odoo>
    <!-- Heredar la acción de ubicaciones para aplicar filtro por defecto -->
    <!-- <record id="view_stock_location_filter_custom" model="ir.ui.view">
        <field name="name">stock.location.search.custom</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_search"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <filter name="my_locations" string="Mis Ubicaciones" 
                        domain="[('id', 'in', user.location_ids.ids)]"/>
                <separator/>
            </xpath>
        </field>
    </record> -->

    <!-- 4. Modificar la acción de ubicaciones para usar el filtro por defecto -->
    <record id="stock_action_location_form_inherit" model="ir.actions.act_window">
        <field name="name">Ubicaciones</field>
        <field name="res_model">stock.location</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_my_locations': 1}</field>
        <field name="search_view_id" ref="view_stock_location_filter_custom"/>
    </record>

    <!-- 5. Vista para asignar ubicaciones a usuarios -->
    <!-- <record id="view_users_form_location_inherit" model="ir.ui.view">
        <field name="name">res.users.form.location.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='access_rights']" position="after">
                <page string="Permisos de Inventario">
                    <group>
                        <field name="warehouse_ids" widget="many2many_tags"/>
                        <field name="location_ids" widget="many2many_tags"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record> -->

</odoo>