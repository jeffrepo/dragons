<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Regla: solo ver los almacenes asignados -->
        <!-- <record id="rule_stock_warehouse_user_access" model="ir.rule">
            <field name="name">Restricción de almacenes por usuario</field>
            <field name="model_id" ref="stock.model_stock_warehouse"/>
            <field name="global" eval="False"/>
            <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
            <field name="domain_force">[('id', 'in', user.warehouse_ids.ids)]</field>
        </record>
 -->
        <!-- Regla: solo ver las ubicaciones asignadas -->
        <!-- <record id="rule_stock_location_user_access" model="ir.rule">
            <field name="name">Restricción de ubicaciones por usuario</field>
            <field name="model_id" ref="stock.model_stock_location"/>
            <field name="global" eval="False"/>
            <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
            <field name="domain_force">[('id', 'in', user.location_ids.ids)]</field>
        </record> -->

        <record id="rule_stock_quant_user_access" model="ir.rule">
            <field name="name">Restricción de quants por ubicación del usuario</field>
            <field name="model_id" ref="stock.model_stock_quant"/>
            <field name="global" eval="False"/>
            <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
            <field name="domain_force">[('location_id', 'in', user.location_ids.ids)]</field>
        </record>

        <record id="rule_stock_picking_user_access" model="ir.rule">
            <field name="name">Restricción de picking por ubicación del usuario</field>
            <field name="model_id" ref="stock.model_stock_picking_type"/>
            <field name="global" eval="False"/>
            <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
            <field name="domain_force">[('warehouse_id', 'in', user.warehouse_ids.ids)]</field>
        </record>

        <record id="rule_product_product_user_access" model="ir.rule">
            <field name="name">Restricción de producto por ubicación del usuario</field>
            <field name="model_id" ref="product.model_product_product"/>
            <field name="global" eval="False"/>
            <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
            <field name="domain_force">[('warehouse_id', 'in', user.warehouse_ids.ids)]</field>
        </record>


    </data>
</odoo>
